import React from 'react';

export default function About() {
  // This acts as a scroll target anchor for the navbar links.
  // The actual About collage rendering and GSAP animations are handled
  // as a seamless continuation of the Hero section.
  return <div id="about" style={{ height: '1px', background: 'transparent' }} />;
}
